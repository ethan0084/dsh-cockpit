# Changelog

## 0.2.3 - 2026-08-16

- 修正发布包内 README 标题仍显示旧版本号的问题：标题不再写死版本，npm 页面本身已展示版本。
- 无功能或安全性变更；`0.2.2` 的安全修复同样适用于本版本。

## 0.2.2 - 2026-08-16

安全修复版本，建议所有用户升级。

- 修复终端 `cwd` 未经工作区边界校验的问题：此前传入 `../../../../etc` 之类的相对路径可在工作区外执行命令，现改为与其他接口一致使用 `targetFrom()` 校验。
- 命令执行后上报的 `cwd` 现会被约束在工作区内，`cd /` 等操作不再能把工作区外的目录带入下一条命令。
- 为 `reveal`、`write`、`transfer`、`upload` 补上同源校验，此前仅 `terminal` 一个接口受保护，任意网页均可跨站改写或上传工作区文件。
- Markdown 预览的链接改为经 `safeUrl()` 过滤，阻断 `javascript:`、`data:` 等可执行协议导致的 XSS。
- 静态资源响应增加 `X-Content-Type-Options: nosniff` 与 `Content-Security-Policy`，SVG 改为以附件形式下载，避免同源内联执行脚本。

## 0.2.1 - 2026-08-16

- 将存在 CVE-2023-30533 与 CVE-2024-22363 的 npm `xlsx@0.18.5` 替换为 SheetJS 官方 CDN 发布的 `xlsx@0.20.3`，并将官方模块 vendor 到发布包以兼容 DSH 供应链策略。
- 工作簿预览改为异步 Buffer 解析，并为 Office 预览增加 25 MB 文件大小上限。

## 0.2.0 - 2026-08-15

- 项目正式更名为 DSH Cockpit；npm 入口改为 `dsh-cockpit`，安装 Skill 改为 `dsh-cockpit-install`。
- 在中部预览区下方新增可展开终端，可从当前项目目录运行命令并保留 `cd` 后的路径。
- 增加终端输出大小、运行时长和同源请求限制。
- 避免终端按钮与 AI 工作区折叠按钮重合，并精简为纯图标显示。
- 文件树单击改为复用一个 VS Code 风格的预览标签，双击或编辑后固定。
- 从 AI 对话打开的工作区文件改为固定到新的中部标签页。
- 首次启用新标签模型时自动收拢旧版积累的文件树标签，无需逐个关闭。

## 0.1.1 - 2026-08-15

- 更名前的兼容性版本。
- 修复 pnpm 隔离依赖结构下，DSH 无法从 profile 根目录加载布局和界面插件的问题。
- 将布局和界面组件内嵌到入口包的公开子路径，保持一条命令即可安装。

## 0.1.0 - 2026-08-15

- 更名前的首次开源版本。
- 提供项目侧栏、文件工作台、预览、AI 上下文和历史会话。
- 将自定义布局封装为 Cordis 插件，不再直接修改 DSH 依赖文件。
- 兼容 DeepSeek Harness 0.1.0-rc.6。
