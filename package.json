{
  "name": "dsh-workbench-source",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo for the DSH Workbench Cordis plugin bundle",
  "license": "MIT",
  "type": "module",
  "packageManager": "pnpm@11.19.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "pack:all": "mkdir -p dist && pnpm --filter deepseek-harness-workbench-layout pack --pack-destination dist && pnpm --filter deepseek-harness-workbench-ui pack --pack-destination dist && pnpm --filter deepseek-harness-workbench pack --pack-destination dist"
  },
  "engines": {
    "node": ">=22"
  }
}
