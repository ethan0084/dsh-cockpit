{
  "name": "dsh-cockpit-source",
  "version": "0.2.0",
  "private": true,
  "description": "Monorepo for the DSH Cockpit Cordis plugin bundle",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ethan0084/dsh-cockpit.git"
  },
  "homepage": "https://github.com/ethan0084/dsh-cockpit#readme",
  "bugs": {
    "url": "https://github.com/ethan0084/dsh-cockpit/issues"
  },
  "type": "module",
  "packageManager": "pnpm@11.19.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "pack:release": "mkdir -p dist && pnpm --filter dsh-cockpit pack --pack-destination dist"
  },
  "engines": {
    "node": ">=22"
  }
}
